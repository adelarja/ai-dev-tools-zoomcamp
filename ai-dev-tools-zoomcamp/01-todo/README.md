Github issue with the instructions: [Exercise 1](https://github.com/adelarja/ai-dev-tools-zoomcamp/issues/1)

## Summary of answers for the homework form

Q1 (Install Django – command used):

```bash
pip install django
```

Q2 (File to include the app in the project):
`settings.py`

Q3 (Next step after creating models):
Run migrations
```bash
python manage.py makemigrations
python manage.py migrate
```

Q4 (Where to put the TODO logic):
`views.py`

Q5 (Where to register the templates directory):
`TEMPLATES['DIRS']` in project's `settings.py`

Q6 (Command to run tests):

```bash
python manage.py test
```
